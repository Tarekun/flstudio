defaults:
  - _self_

sim_cfg:
  num_rounds: 10
  num_cpus: 2
  num_gpus: 0.0

train_cfg:
  epochs: 10
  lr: 0.01
  momentum: 0.9

data_cfg:
  num_clients: 9
  hybrid_ratio: 0.0
  batch_size: 32
  dataset: har
  num_classes: 6

hydra:
  run:
    dir: .
